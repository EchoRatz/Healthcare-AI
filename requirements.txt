# 📦 AI Query System - Requirements
# ไม่ต้องการ external dependencies สำหรับการทำงานพื้นฐาน
# ใช้ Python standard library เท่านั้น

# Optional dependencies สำหรับฟีเจอร์เพิ่มเติม:
# requests>=2.28.0    # สำหรับ web search (ถ้าต้องการ)
# numpy>=1.21.0       # สำหรับ advanced vector operations
# pandas>=1.3.0       # สำหรับ CSV processing ขั้นสูง

# Development dependencies:
# pytest>=7.0.0       # สำหรับการทดสอบ
# black>=22.0.0       # สำหรับ code formatting
# mypy>=0.950         # สำหรับ type checking

# Note: โปรเจคนี้ออกแบบให้ทำงานได้ด้วย Python standard library
# ไม่จำเป็นต้องติดตั้ง dependencies เพิ่มเติม
